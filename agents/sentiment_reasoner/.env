# .env.example

# Agent identity
AGENT_NAME=sentiment-reasoner
REASONER_SEED=reasoner-demo-seed
USE_MAILBOX=true

# Optional: path to MeTTa rules (used only if Hyperon is installed)
METTA_RULES=rules/rebalance_rules.metta