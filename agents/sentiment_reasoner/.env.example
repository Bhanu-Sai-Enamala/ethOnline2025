# ───────────────────────────────────────────────
# Sentiment Reasoner Agent Configuration
# ───────────────────────────────────────────────

# Unique human-readable name for the agent
AGENT_NAME=sentiment-reasoner

# Deterministic seed for wallet/address generation
REASONER_SEED=reasoner-demo-seed

# Enable or disable mailbox messaging via Agentverse
USE_MAILBOX=true

# Path to your MeTTa rules file (for Hyperon)
METTA_RULES=rules/rebalance_rules.metta